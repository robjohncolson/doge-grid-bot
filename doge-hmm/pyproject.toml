[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "doge-hmm"
version = "0.1.0"
description = "Rust HMM backend for DOGE regime detection"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
